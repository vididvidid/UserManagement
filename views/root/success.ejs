<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donation Receipt</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        h1 {
            color: #2c3e50;
        }
        .success {
            color: #27ae60;
        }
        .failure {
            color: #c0392b;
        }
        .details {
            margin-top: 20px;
            border: 1px solid #ddd;
            padding: 20px;
            border-radius: 5px;
        }
        .download-btn {
            display: inline-block;
            background-color: #3498db;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <h1>Donation Receipt</h1>
    
    <% if (success) { %>
        <h2 class="success"><%= message %></h2>
    <% } else { %>
        <h2 class="failure"><%= message %></h2>
    <% } %>
    
    <% if (donationDetails) { %>
        <div class="details">
            <h3>Donation Details:</h3>
            <p><strong>Order ID:</strong> <%= donationDetails.orderId %></p>
            <p><strong>Amount:</strong> <%= donationDetails.amount %></p>
            <p><strong>Status:</strong> <%= donationDetails.status %></p>
            <p><strong>Payment ID:</strong> <%= donationDetails.paymentId %></p>
            <p><strong>Date:</strong> <%= donationDetails.createdAt.toLocaleString() %></p>
        </div>
    <% } %>
    
    <a href="#" class="download-btn" onclick="window.print()">Download as PDF</a>

    <script>
        // You can add any additional JavaScript here if needed
    </script>
</body>
</html>